# Backend origin only. The server adds /api. This variable is never public.
EXPENSE_TRACKER_CORE_URL=https://api--expense-tracker-core--j6xvqz9kpswd.code.run
# For a separately configured local backend, use http://localhost:6000 instead.

# Optional: disable Next.js telemetry during local development.
NEXT_TELEMETRY_DISABLED=1
